kind: Service 
apiVersion: v1
metadata:
  name: mongodb-svc
  namespace: mongoshop-database 
  labels:
    app: mongo
spec:
  clusterIP: None
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
      name: mongo 
