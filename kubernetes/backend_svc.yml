kind: Service
apiVersion: v1
metadata:
  name: backend-service
  namespace: mongoshop-backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      nodePort: 31000

